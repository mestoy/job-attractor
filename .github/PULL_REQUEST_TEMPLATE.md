## What this does and why

<!-- One or two sentences. -->

## Checklist

- [ ] Only files outside `documents/` are touched
- [ ] `scripts/kit_config.py` is not in this diff (only `scripts/kit_config.example.py` ships here)
- [ ] `bash tests/run_all.sh` was run locally; its last line is pasted below
- [ ] No real names or absolute home-directory paths anywhere in the diff (code, comments, fixtures, or commit messages)
- [ ] This PR is one change, not several bundled together

## Last line of `bash tests/run_all.sh`

```
<paste here>
```
